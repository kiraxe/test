
            const $ = require('jquery');
            const str = require('./constant');

            const App = function() {
                $(body).html(str);
            };

            const app = new App();
            app.get = function() {
            
            };
            console.log();
        